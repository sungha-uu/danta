# Danta 프로젝트 에이전트 지침

이 저장소에서 작업하는 모든 AI 개발 에이전트는 작업 전에 반드시 [`DOCS/agent.md`](DOCS/agent.md)를 읽는다. 해당 문서의 읽기 순서와 정책 우선순위를 따른다.

## 최우선 세션 운영 원칙

이 프로젝트는 현재 사용자와 이어지는 이 Codex 세션을 중심으로 개발한다. 에이전트는 매 사용자 메시지에서 새 요구사항, 변경된 결정, 폐기된 전제, 미해결 항목을 식별하고 코드 작업 전에 관련 기준 문서를 갱신한다.

- 확정된 요구사항은 대화에만 남겨두지 않는다.
- 새 파일 생성보다 기존 기준 문서 수정을 우선한다.
- 같은 규칙을 여러 문서에 복제하지 않고 하나의 기준 문서와 링크로 관리한다.
- 기존 내용과 충돌하는 새 결정이 나오면 이전 내용을 함께 수정하거나 삭제한다.
- 더 이상 사용하지 않는 문서는 병합·보관·삭제하고 활성 인덱스에서 제거한다.
- 문서 변경 후 로컬 링크, 활성 브로커, 정책 충돌을 검사한다.
- 단순 질문·아이디어는 즉시 명세로 확정하지 않고 사용자가 승인한 결정만 불변 규칙으로 올린다.
- 문제 해결로 코드·설정·운영절차가 바뀌면 `DOCS/TROUBLESHOOTING.md`에 원인·해결·검증·재발방지를 누적한다.
- AI 작업 전 Codex Plus 잔여량 또는 별도 API 예산 상태를 확인하고, 사용량 출처·기간·측정시각을 운영 기록에 남긴다.

## 변경 불가능한 핵심 규칙

- 활성 증권사 연동은 **한국투자증권 Open API(KIS)**다. 키움증권 자료와 과거 원본은 비교·보관 자료일 뿐 활성 구현 기준으로 사용하지 않는다.
- 시가총액 상위 200개 분석 종목은 모두 공식 주문 후보이며 대시보드에서 선택할 수 있다. `박스 하단 접촉 후 3거래일 이내 +10% 도달` 등 정량 자격은 추천·위험 정보로만 사용하고 선택 권한을 잠그지 않는다. AI 심층검토는 14일 정량 순위 상위 50개에 수행하며 나머지 150개는 정량 정보만 제공한다.
- 사용자가 관찰 종목 1~3개를 지정하기 전에는 실시간 집중 감시나 주문을 시작하지 않는다.
- 사용자는 대시보드에서 1~3개 종목·목표가·주문가능현금 배정률을 정하고 생성된 `ENTRY_MANDATE` 승인문을 Android Codex에 붙여넣어 자동 진입을 승인한다. 배정률은 1종목 100.0%, 2종목 각 50.0%, 3종목 각 33.3%가 기본이며 사용자가 소수점 한 자리까지 수정할 수 있고 실제 수량은 KIS 주문가능금액 조회로 계산한다.
- `ENTRY_MANDATE`의 목표가는 즉시 체결 명령이 아니라 절대 초과할 수 없는 최대 매수가다. 가격 도달 후 매도 압력이 강하면 로컬 진입 엔진이 기다리고, 하락 안정·매수세 회복이 확인될 때만 목표가 이하 지정가로 매수하며 박스·시장·승인이 무효화되면 더 낮은 가격이어도 주문하지 않는다.
- 자동매수·자동매도 엔진은 종목별 순수 결정을 만들고 중앙 `TradingOrchestrator`가 다종목 actor, 자금 예약, 주문 충돌과 우선순위를 조정한다. 현재 승인 한도는 최대 3개지만 내부 구조는 검증된 `N`개로 확장 가능해야 하며, 중앙 코어도 `-7%` 강제 손절을 거부하거나 지연할 수 없다.
- 대시보드 코멘트나 종목 선택만으로는 매수를 승인한 것이 아니며 구조화된 `ENTRY_MANDATE` 승인문 전달이 필요하다.
- GitHub Pages 코멘트는 분석 메모다. 사용자가 Android Codex 앱에서 이 PC 에이전트에게 확인·작업을 지시한 뒤에만 로컬 명령으로 정규화한다.
- 기본 경로에서는 사용자 승인 없는 자율 매수와 물타기를 금지한다. 단, 사용자가
  별도로 승인한 `PAPER_AUTONOMOUS_CAMPAIGN`은 KIS 모의투자 계좌에 한해 캠페인
  만료 전까지 종목별 `ENTRY_MANDATE` 재승인 없이 신규매수를 허용한다. 이 권한은
  실계좌에서 설정과 무관하게 거부하며, 물타기는 캠페인에서도 금지한다.
- `PAPER_AUTONOMOUS_CAMPAIGN`은 승인 시점부터 최대 90일, 최대 동시 3종목,
  에이전트 심층검토 결과가 `적극 추천` 또는 `추천`인 종목으로 제한한다. 시장 위험,
  일일 신규 배치 한도, 데이터 신선도, 주문·잔고 불일치, 로컬 중지 스위치 중 하나라도
  실패하면 신규매수를 차단한다.
- 캠페인 만료·중지 시 신규매수만 중단한다. 기존 포지션의 보호 감시, `-7%` 강제
  손절, 승인된 익절, 체결·잔고대조와 매도 알림은 전량 청산될 때까지 유지한다.
- 평균 체결가 대비 `-7%` 도달 시 사용자 재확인 없이 매도 가능 수량 전부를 즉시 시장가로 손절한다.
- 익절에는 고정 불변 수익률을 두지 않는다. AI가 모의투자 성과를 분석해 버전된 적응형 수익관리 로직을 제안하고, 승인된 로컬 엔진이 장중 실행하되 `-7%` 손절을 취소하거나 지연할 수 없다.
- 비밀정보, 계좌정보, 주문정보를 Git이나 GitHub Pages에 기록하지 않는다.
- 실계좌 기능은 모의투자·소액 실전·장애 시나리오 검증 전에는 활성화하지 않는다.

## 작업 원칙

- 구현 전 대화와 관련 기준 문서를 대조하고, 문서를 먼저 또는 같은 변경에서 갱신한다.
- 산식·임계값·프롬프트·모델·데이터 버전을 기록한다.
- 외부 API의 필드와 한도는 추정하지 말고 KIS Developers 공식 가이드·공식 GitHub 샘플과 실제 `provider-doctor` 진단 결과로 확인한다.
- 주문 경로는 멱등성, 재시작 복구, 잔고 대조, 감사 로그를 갖춰야 한다.
- 실시간 주문 판단이 외부 LLM 응답을 기다리게 만들지 않는다.
- 장애 시 신규매수는 차단할 수 있지만 열린 포지션의 보호 감시와 -7% 매도 경로는 중단하지 않는다.
- 원인을 확인하지 않은 단순 재시도 성공을 해결 완료로 기록하지 않는다.
- AI 사용량이 경고 단계면 정량 폴백과 저비용·저빈도 모드로 전환하되 손절·익절·잔고대조는 축소하지 않는다.
- 모든 Python 자동화 모듈은 `DOCS/QUALITY_AND_VALIDATION.md`의 관련 체크와 검증 증거를 갖춰야 완료로 처리한다.
- 매 거래일 품질을 점검하되 실전 알고리즘을 일일 성과에 따라 자동 수정하지 않는다.
- 실전 전환 후에도 실전 안정판·모의 그림자·모의 개선판을 분리해 계속 운용한다.
- 전략·주문·위험 로직 변경은 모의 개선판에서 검증하고 사용자 승인 후에만 실전 안정판으로 승격한다.
- 치명 체크 실패 시 신규매수는 차단하되 기존 보유 포지션의 보호 감시와 자동매도는 유지한다.
- 일일 품질 보고와 이메일은 AI 할당량에 의존하지 않는 로컬 Python 경로로 구현한다.
- 충돌 시 [`DOCS/agent.md`](DOCS/agent.md)의 정책 우선순위를 따른다.
